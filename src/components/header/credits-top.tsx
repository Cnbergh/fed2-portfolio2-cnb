'use client';

import { useRouter } from "next/navigation";

const CreditsTop = () => {
  const router = useRouter();
  return (
    <div className="">
CreditsTop
    </div>
  )
}

export default CreditsTop;